{% set search = craft.entries.section('search').one() %}

<section class="blocks blocks--search" data-show="$searchOpen == true">
  <div class="blocks__content">
    <div class="bg-stone-200 rounded-md p-8">
      <p class="ui-bold">{{ search.title }}</p>
      {{ search.render({
        type: 'modal',
      }) }}
    </div>
  </div>
</section>

